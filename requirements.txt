# pip fallback for users who do not use uv. The canonical, locked environment is
# defined in pyproject.toml + uv.lock (`uv sync`). Optional extras (HMM/GARCH,
# seaborn, jupyter, dev tooling) live in pyproject.toml [project.optional-dependencies].
numpy>=2.0
pandas>=2.2
scipy>=1.13
statsmodels>=0.14
scikit-learn>=1.5
matplotlib>=3.9
pyarrow>=16.0
requests>=2.32
tenacity>=8.3
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
loguru>=0.7
pyyaml>=6.0
tqdm>=4.66
rich>=13.7
